package org.mytonwallet.app_air.walletcontext.theme

object ViewConstants {
    // Used wherever we need a radius, no matter what's the active theme
    var STANDARD_ROUNDS = 16f

    // BAR_ROUNDS identifies the rounding of the top and bottom toolbars
    var BAR_ROUNDS = 16f

    // The radius around the items, based on the active theme
    var BIG_RADIUS = 16f

    // TOP_RADIUS is used for the first item below the navigation bar
    var TOP_RADIUS = 16f

    // The gap between items in the screens
    var GAP = 12

    // The paddings on the left and right, when Side Gutters are on
    var HORIZONTAL_PADDINGS = 10
}
